import styled from "styled-components";

type Props = {

};
export const Music = (props: Props) => {
    return (
        <StylesMusic>
Music
        </StylesMusic>
    );
};

const StylesMusic = styled.div`
    
`