import styled from "styled-components";

type Props = {};
export const Settings = (props: Props) => {
    return (
        <StylesSettings>
            Settings
        </StylesSettings>
    );
};

const StylesSettings = styled.div`

`