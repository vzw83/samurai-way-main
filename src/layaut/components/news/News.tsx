import styled from "styled-components";

type Props = {};
export const News = (props: Props) => {
    return (
        <StylesNews>
            News

        </StylesNews>
    );
};

const StylesNews = styled.div`

`